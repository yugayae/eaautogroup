# 🎨 Руководство по анимациям Eaautogroup

## 📁 Структура файлов

```
JS/
├── animations.js          # Главный файл анимаций
├── main.js               # Основной функционал
├── api/
│   └── currency-api.js   # API для валют
├── calculators/          # Калькуляторы для разных стран
│   ├── calc-russia.js
│   ├── calc-belarus.js
│   ├── calc-ukraine.js
│   ├── calc-kazakhstan.js
│   └── calc-kyrgyzstan.js
└── features/             # Дополнительные функции
    ├── car-selection.js  # Подбор автомобилей по маркам/моделям
    └── tiktok-videos.js  # TikTok видеообзоры
```

## 🚀 Порядок подключения скриптов

```html
<!-- 1. API для валют (должен быть первым) -->
<script src="JS/api/currency-api.js"></script>

<!-- 2. Калькуляторы (все страны) -->
<script src="JS/calculators/calc-russia.js"></script>
<script src="JS/calculators/calc-belarus.js"></script>
<script src="JS/calculators/calc-ukraine.js"></script>
<script src="JS/calculators/calc-kazakhstan.js"></script>
<script src="JS/calculators/calc-kyrgyzstan.js"></script>

<!-- 3. Основной функционал -->
<script src="JS/main.js"></script>

<!-- 4. Анимации (последний, чтобы не конфликтовать) -->
<script src="JS/animations.js"></script>
```

## ✨ Доступные анимации

### 1. Анимации при скролле
```html
<div class="animate-on-scroll" data-delay="200">
  <!-- Контент появится с задержкой 200ms -->
</div>
```

### 2. Ripple эффект для кнопок
Автоматически применяется к классам: `.btn`, `.cta-button`, `.submit-btn`

### 3. Hover эффекты для карточек
Автоматически применяется к классам: `.about-card`, `.contact-card`, `.advantage`

### 4. Анимации навигации
Автоматически применяется к ссылкам в `nav a`

### 5. Анимации иконок
Автоматически применяется к заголовкам `h3`

## 🎯 Использование

### Добавление анимации к новому элементу:
```html
<div class="animate-on-scroll" data-delay="0">
  <h2>Заголовок</h2>
  <p>Текст появится сразу</p>
</div>

<div class="animate-on-scroll" data-delay="300">
  <p>Этот текст появится через 300ms</p>
</div>
```

### Создание анимированной кнопки:
```html
<button class="btn">Кнопка с ripple эффектом</button>
```

### Создание анимированной карточки:
```html
<div class="about-card">
  <h2>Заголовок</h2>
  <p>Содержимое карточки</p>
</div>
```

## 🔧 Настройка

### Изменение скорости анимации:
В `style.css` найдите:
```css
.animate-on-scroll {
  transition: all 0.8s ease-out; /* Измените 0.8s на нужное значение */
}
```

### Добавление новых анимаций:
В `JS/animations.js` добавьте новую функцию в `initAllAnimations()`:
```javascript
function initAllAnimations() {
    initScrollAnimations();
    initButtonAnimations();
    initCardAnimations();
    initPageLoadAnimation();
    initNavigationAnimations();
    initIconAnimations();
    initSmoothScroll();
    initCounterAnimations();
    // Добавьте здесь вашу новую функцию
    initYourNewAnimation();
}
```

## 🎨 CSS классы для анимаций

- `.animate-on-scroll` - анимация появления при скролле
- `.animate-fade-in` - плавное появление
- `.animate-slide-down` - появление сверху
- `.animate-pulse` - пульсация
- `.animate-bounce` - подпрыгивание
- `.ripple` - эффект волны для кнопок

## 📱 Совместимость

- ✅ Chrome 60+
- ✅ Firefox 55+
- ✅ Safari 12+
- ✅ Edge 79+
- ✅ Мобильные браузеры

## 🐛 Отладка

Если анимации не работают:

1. Проверьте порядок подключения скриптов
2. Убедитесь, что `JS/animations.js` подключен последним
3. Проверьте консоль браузера на ошибки
4. Убедитесь, что элементы имеют правильные CSS классы

## 📞 Поддержка

При возникновении проблем проверьте:
- Консоль браузера (F12)
- Правильность путей к файлам
- Порядок подключения скриптов
